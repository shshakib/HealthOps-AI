* text=auto eol=lf
*.png binary
*.jpg binary
*.zip binary
# Raw registry responses are hashed byte-for-byte; never normalize their bytes.
data/clinicaltrials/**/study.json -text
# Preserve the published evaluation dataset's byte-for-byte fingerprint across platforms.
src/healthops/evaluation_cases.json -text
data/evaluation/*.json -text
